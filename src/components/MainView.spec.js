import add from './testable'

describe('MainView.vue', () => {
  test('setup correctly', () => {
    expect(true).toBe(true)
  })
  test('sanity test', () => {
    expect(1 + 1).toBe(2)
  })
})
